<div class="row">
  <div class="col-sm-12">
    <h1><%= t ".categories" %> <%= link_to 'Add category', new_admin_category_path, class: 'btn btn-success' %></h1>
  </div>
</div>

<hr />

<table class="table table-striped">
  <tr>
    <th>Title</th>
    <th style="width: 100px;">Actions</th>
  </tr>

  <% @categories.each do |category| %>
      <tr>
        <td><%= category.name_en %></td>
        <td>
          <%= link_to 'Delete', admin_category_path(category), method: :delete, data: {confirm: 'Are you sure?'} %>
        </td>
      </tr>
  <% end %>
</table>
<table class="table table-striped">
  <tr>
    <th>Название</th>
    <th style="width: 100px;">Действия</th>
  </tr>

  <% @categories.each do |category| %>
      <tr>
        <td><%= category.name_ru %></td>
        <td>
          <%= link_to 'Удалить', admin_category_path(category), method: :delete, data: {confirm: 'Вы уверены?'} %>
        </td>
      </tr>
  <% end %>
</table>
